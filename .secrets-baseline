{
  "_comment": "Known findings, excused so the scanner fails only on NEW ones. An entry is a sha256 prefix of the matched value, never the value. ROTATE the credential and delete its entry \u2014 the two belong in one diff.",
  "findings": [
    "bin/backup.sh::rfc1918::3d56985a7fb178e8"
  ]
}
