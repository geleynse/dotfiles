*_local
vim/undodir/*
!vim/undodir/.gitignore
.DS_Store
__pycache__/
*.pyc

# restic credentials — never commit these
config/restic/env
config/restic/password

# large binaries
bin/orca-slicer

# scripts/ untracked 2026-05-29 — private/infra tooling, do not publish (lives on disk + synced separately)
/scripts/
